$("#results").html("<% @contributions.each do |contribution| %><%= j render contribution %><% end %>");
var providers = ("<%= @providers.to_json %>").replace(/&quot;/g,'"');

$("#map-canvas").data("providers",JSON.parse(providers));
initMap();
